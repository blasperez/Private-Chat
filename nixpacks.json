{
  "providers": ["node"],
  "buildPlan": {
    "phases": {
      "setup": {
        "nixPkgs": ["nodejs_20"]
      },
      "install": {
        "cmds": ["npm ci"]
      },
      "build": {
        "cmds": ["npm run build:client"]
      }
    }
  },
  "start": {
    "cmd": "npx vite preview --port ${PORT:-3000} --host 0.0.0.0"
  }
}